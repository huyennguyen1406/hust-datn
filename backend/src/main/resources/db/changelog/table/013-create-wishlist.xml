<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="013-create-wishlist" author="huyennguyen1406" dbms="postgresql">
        <preConditions>
            <not>
                <tableExists tableName="wishlist"/>
            </not>
        </preConditions>

        <createTable tableName="wishlist">
            <column name="id" type="uuid" defaultValueComputed="uuid_generate_v4()">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="uuid">
                <constraints nullable="false"/>
            </column>

            <column name="product_id" type="uuid">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="timestamp">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Foreign keys -->
        <addForeignKeyConstraint
                baseTableName="wishlist"
                baseColumnNames="user_id"
                referencedTableName="app_user"
                referencedColumnNames="id"
                constraintName="fk_wishlist_user"/>

        <addForeignKeyConstraint
                baseTableName="wishlist"
                baseColumnNames="product_id"
                referencedTableName="product_detail"
                referencedColumnNames="id"
                constraintName="fk_wishlist_product"/>

        <!-- Optional: prevent duplicate wishlist items -->
        <addUniqueConstraint
                tableName="wishlist"
                columnNames="user_id, product_id"
                constraintName="uk_wishlist_user_product"/>

    </changeSet>

</databaseChangeLog>
